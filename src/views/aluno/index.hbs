{{#> main}}
{{#*inline "content"}}
{{#if flashErrors}}
<div class='alert alert-danger'>
    {{#each flashErrors}}
    <p style="margin: 0; padding: 0;"> {{this}} </p>
    {{/each}}
</div>
{{/if}}
<div class="card w-100">
    <div class="card-body p-4">
        <div class="row">
            <div class="col col-10">
                <h5 class="card-title fw-semibold mb-4">Alunos</h5>
            </div>
            <div class="col">
                <a href="/alunos/novo" class="btn btn-primary">
                    <i class="ti ti-plus"></i>
                    <span>Aluno</span>
                </a>
            </div>
        </div>

        <div class="table-responsive">
            {{#unless alunos}}
            <h4>Nenhum aluno encontrado na base de dados!</h4>
            {{/unless}}
            {{#if alunos}}
            <table class="table text-nowrap mb-0 align-middle">
                <thead class="text-dark fs-4">
                    <tr>
                        <th class="border-bottom-0">
                            <h6 class="fw-semibold mb-0">#</h6>
                        </th>
                        <th class="border-bottom-0">
                            <h6 class="fw-semibold mb-0">Nome</h6>
                        </th>
                        <th class="border-bottom-0">
                            <h6 class="fw-semibold mb-0">CPF</h6>
                        </th>
                        <th class="border-bottom-0">
                            <h6 class="fw-semibold mb-0">Data de Nascimento</h6>
                        </th>
                        <th class="border-bottom-0">
                            <h6 class="fw-semibold mb-0">Opções</h6>
                        </th>
                    </tr>
                </thead>
                <tbody>
                    {{#each alunos}}
                    <tr>
                        <td class="border-bottom-0">
                            <h6 class="fw-semibold mb-0">{{inc @index}}</h6>
                        </td>
                        <td class="border-bottom-0">
                            <h6 class="fw-semibold mb-1">{{this.nome}}</h6>
                            <span class="fw-normal">Aluno</span>
                        </td>
                        <td class="border-bottom-0">
                            <p class="mb-0 fw-normal">{{this.cpf}}</p>
                        </td>
                        <td class="border-bottom-0">
                            <p class="mb-0 fw-normal">{{formatDate this.dataNascimento}}</p>
                        </td>
                        <td class="border-bottom-0">
                            <a href="/alunos/{{this.id}}/atualizacao" class="btn btn-warning">
                                <i class="ti ti-pencil"></i>
                            </a>
                            <a href="/alunos/{{this.id}}/exclusao" class="btn btn-danger">
                                <i class="ti ti-trash"></i>
                            </a>
                        </td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
            {{/if}}
        </div>
    </div>
</div>
{{/inline}}
{{/main}}